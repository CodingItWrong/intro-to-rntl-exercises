import React from 'react';
import { render } from 'react-native-testing-library';
import WidgetContainer from '../WidgetContainer';

describe.skip('WidgetContainer', () => {
  it('loads widgets upon mount', () => {
    fail('implement this test');
  });
});
