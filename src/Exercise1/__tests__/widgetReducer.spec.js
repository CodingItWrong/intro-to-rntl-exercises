import widgetReducer from '../widgetReducer';

describe.skip('widgetReducer', () => {
  describe('ADD_WIDGET', () => {
    it('adds the widget to the end of the list', () => {
      fail('implement this test');
    });

    it('does not add the widget if there is already one with the same ID', () => {
      fail('implement this test');
    });
  });

  describe('unrecognized actions', () => {
    it('returns the state unchanged', () => {
      fail('implement this test');
    });
  });
});
